{
  "name": "at-sim",
  "private": true,
  "version": "0.1.0",
  "description": "Auto tuning simulator — Hyundai/Kia vehicle and parts catalogs",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @at-sim/parts && npm run build -w @at-sim/vehicles && npm run build -w @at-sim/web",
    "dev": "npm run dev -w @at-sim/web",
    "test": "npm run test -w @at-sim/parts && npm run test -w @at-sim/vehicles",
    "demo": "npm run demo -w @at-sim/vehicles",
    "preview": "npm run preview -w @at-sim/web"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20"
  }
}
